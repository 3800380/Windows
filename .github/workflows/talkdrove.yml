name: TalkDrove

on:
  workflow_dispatch:

jobs:
  build:
    name: TalkDrove working
    runs-on: windows-latest
    steps:
      - name: TalkDrove Downloading & Installing Essentials
        run: |
          Invoke-WebRequest -Uri "https://www.dropbox.com/scl/fi/5lhbbd3l2xuxloyxg0hwb/Downloads.bat?rlkey=jrt26tfl76zeq1ednf2h8lcan&st=j7pcb5lf&dl=1" -OutFile "Downloads.bat"
          cmd /c Downloads.bat

      - name: TalkDrove login
        run: cmd /c show.bat
      - name:   Timer
        run: python time.py

